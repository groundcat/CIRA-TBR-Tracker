name: Fetch TBR Session Results

on:
  schedule:
    # Every Wednesday at 19:30 UTC — 30 min after the CIRA TBR session opens
    - cron: '30 19 * * 3'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run — fetch & analyze but do not commit'
        required: false
        default: 'false'

permissions:
  contents: write

jobs:
  fetch-and-analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Create venv and install dependencies
        run: |
          python -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -r requirements.txt

      - name: Fetch, archive, analyze, and generate charts
        run: .venv/bin/python scripts/analyze.py

      - name: Commit and push results
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          SESSION_DATE=$(date -u '+%Y-%m-%d')
          git config user.name  "TBR Bot"
          git config user.email "tbr-bot@users.noreply.github.com"
          git add data/ tally.json README.md charts/
          git diff --staged --quiet \
            && echo "Nothing changed — session may already be recorded." \
            || git commit -m "data: TBR session ${SESSION_DATE}"
          git push
